# Roadmap & status

The living record of what's built and what's next. **Every PR/sprint that completes an item must update this file in the same commit** — that's how future contributors (and AI sessions) know where to pick up. Deeper context: [PRD](PRD.md) for product scope, [ADR-0001](adr/0001-central-slim-catalog.md) for catalog architecture, [docs/design](design/README.md) for the AURA PRISM mockups.

## ✅ Done

| Area         | What                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Ref                                    |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| Foundation   | Monorepo scaffold: apps (web/api/worker), packages (shared/db/providers), Docker monolith, CI                                                                                                                                                                                                                                                                                                                                                              | `22cb05c`                              |
| Design       | AURA PRISM design handoff — 8 mockups (Landing, Login, Home, Search, Media Detail, Lists, Profile, Design System)                                                                                                                                                                                                                                                                                                                                          | `a99582b`, docs/design                 |
| Design       | Tailwind v4 + AURA PRISM tokens + self-hosted fonts wired into apps/web; shared component primitives                                                                                                                                                                                                                                                                                                                                                       | `4a370a9`, `da1de5e`                   |
| Web          | Landing page (AURA PRISM)                                                                                                                                                                                                                                                                                                                                                                                                                                  | `9735f1c`                              |
| Web          | Login + register pages, end-to-end auth flow (better-auth, email/password + username)                                                                                                                                                                                                                                                                                                                                                                      | `9f13a9b`                              |
| Architecture | Central slim catalog pivot: deterministic canonical IDs (UUIDv5), slim media contract, local-Postgres search (`/api/v1/search`), `apps/catalog` service with seq-cursor protocol, dev seed (`pnpm db:seed`)                                                                                                                                                                                                                                               | `495ba65`, ADR-0001                    |
| Architecture | Federated catalog search: search queries local `media` AND the central catalog live (`GET /v1/catalog/search`), merges by canonical id, materializes central-only hits into `media` on first sight — no instance-side full-catalog mirror, no background staleness sync. Replaces the old `catalog-sync` worker job and the bulk `/v1/catalog/changes` feed.                                                                                            | ADR-0002                               |
| Web          | Search/Discover page (`/search`, mockup Search.dc.html): debounced `/api/v1/search` consumer, kind filter chips, shareable `?q=&kind=` URLs, ⌘K focus, `AppNav`; `@trackt/shared` made browser-safe (pure-TS SHA-1 for uuidv5)                                                                                                                                                                                                                             | `apps/web/src/routes/search.tsx`       |
| Core         | Tracking core API: log status, 0–10 half-step ratings, per-episode/chapter check-ins with lazily generated flat `media_part` rows (numbered from slim-catalog totals), auto `in_progress` on first check-in                                                                                                                                                                                                                                                | `apps/api/src/routes/v1/tracking.ts`   |
| Web          | Media detail page (`/media/$slug`, mockup Media Detail.dc.html): hero + check-in/status/rate actions, episode/chapter checklist, community stats, genre-overlap related, details card; search results link to it                                                                                                                                                                                                                                           | `apps/web/src/routes/media.$slug.tsx`  |
| Web          | Home dashboard (`/home`, mockup Home.dc.html): `GET /api/v1/me/home` summary — up-next one-tap check-ins, in-progress shelf with PRISM bars, own recent-activity feed (Friends waits for v1.x follows), this-year stats, fresh-account empty state, nav search pill                                                                                                                                                                                        | `apps/web/src/routes/home.tsx`         |
| Web          | Profile page + favourites (`/profile`, mockup Profile.dc.html): `GET /api/v1/me/profile`, ♥ favourite toggle on media pages, per-kind ranked shelves, stats strip, recent feed; edit profile (name/bio, avatar upload via multipart → `UPLOADS_DIR` served at `/uploads`, social links with per-platform handle→URL normalization); followers/badges/visibility wait for v1.x                                                                              | `apps/web/src/routes/profile.tsx`      |
| Core         | User-created entries (all kinds) + per-instance moderation queue: `POST /api/v1/media` (random UUIDs, `unverified`, 20/day cap) + cover upload, viewer-scoped visibility (unverified → creator+mods, rejected → creator; one rule in `apps/api/src/lib/visibility.ts`), role via better-auth `additionalFields` (`input: false`), `/moderation` queue page (approve/edit/reject/un-reject), create-entry dialog on `/search`, `pnpm db:set-role` bootstrap | `apps/api/src/routes/v1/moderation.ts` |
| Backend      | Audit hardening: `{error}`-shaped error handlers (no leaked internals), `@fastify/rate-limit` on api+catalog (health exempt, tighter `/search` — covers ADR-0002's unauthenticated federated search), APP_URL required in production, race-safe daily entry limit (advisory lock), forward-compatible federated-search parsing (hits with unknown enums skipped, not the whole response), worker fail-fast boot when Redis is down, dedup guard in migration 0005, timing-safe catalog admin token compare, `forceCloseConnections` shutdown; media soft-delete groundwork (`media.deleted_at`, enforced at the visibility seam + own-data joins, federation never resurrects; nothing sets it yet — hard DELETE stays cascade-by-design for purges) | `apps/api/src/lib/error-handler.ts`    |
| Infra        | Catalog service deployment: `apps/catalog/Dockerfile` (turbo-filtered multi-stage build, no Redis/no supervisor — a single self-migrating process), `docker/smoke-test-catalog.sh`, `docker-catalog` CI job (build + smoke-test only — unlike the monolith image, nobody but the project operator pulls this one, so it's never auto-published; push manually when deploying), `apps/catalog/.env.example`, Scaleway+Neon deploy walkthrough (`docs/catalog-hosting.md`)                                                                                                                                                                                                                                                                                                    | `apps/catalog/Dockerfile`              |
| Architecture | Per-season media + single `part_count` (ADR-0003): a `series`/`anime` media is one **season** with its own canonical UUID (`tmdb:series:<showId>:<seasonNumber>`; anime already per-season via AniList), flat with no parent-show row; the four count columns collapse to one nullable `part_count` (+ `season_number`). Straight drop-and-add migrations (pre-launch, no data). Touches both DB schemas, the slim/detail/search/moderation/user-entry contracts, all mappers, the dev seed, and the create/detail/moderation UI                                                                                                                                                                       | ADR-0003                               |

## 🔜 Next (in order)

1. **Catalog population** (deliberately last — relies on importer scripts to be built in a later dedicated sprint) — importers feeding `apps/catalog` from redistributable sources only (anime-offline-database → anime; TVmaze → series; Wikidata → movies; MangaDex/AniList → manga). Single-writer publish path (seq caveat, ADR-0001). Replaces the 501 stub at `POST /v1/admin/media`.

## 📋 Backlog (unordered, from PRD)

- Lists (mockup: `Lists.dc.html`); profile follow-ups: favourite reordering, public profiles + visibility (v1.x)
- Media detail follow-ups: per-episode titles once the catalog carries structure (seasons are now their own media, ADR-0003); episode rating heatmap (v1.x); optional parent "show" grouping over flat seasons
- User-entry follow-ups: merge-into-catalog tool (re-point progress/ratings at the canonical row), creator self-edits while unverified
- TV Time importer; full account export (JSON/CSV)
- Per-instance enrichment (descriptions/posters via instance's TMDB key — repurpose parked `packages/providers`)
- Activity feed, comments, episode rating graphs, airing calendar + notifications (v1.x per PRD §9)

## Decisions to remember

- **No live provider connectors, no scraping** — catalog data flows only through the central slim catalog (ADR-0001).
- Canonical IDs are frozen forever: namespace `f8d11238-d681-551c-875d-5ac53892f6e7`, key `provider:kind:externalId`.
- `packages/providers` is parked, not deleted; `TMDB_API_KEY` reserved for future enrichment.
- **No full-catalog mirror to self-hosted instances** — search is federated (local + live central), central hits materialize into local `media` on first sight, no background re-sync (ADR-0002).
