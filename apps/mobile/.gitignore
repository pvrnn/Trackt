# Expo build output and generated native projects. `ios/`/`android/` are
# generated by prebuild (CNG) and are not checked in — app.config.ts is the
# source of truth for everything they contain.
.expo/
dist/
ios/
android/
expo-env.d.ts
